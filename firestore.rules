rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // テストモード: 30日間すべてのアクセスを許可
    match /{document=**} {
      allow read, write: if request.time < timestamp.date(2025, 2, 15);
    }
  }
}